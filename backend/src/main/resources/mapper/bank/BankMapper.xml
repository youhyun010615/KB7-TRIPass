<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tripass.bank.mapper.BankMapper">

    <delete id="deleteAllBranches">
        DELETE FROM exchange_bank_branches
    </delete>

    <insert id="insertBankBranches" parameterType="java.util.List">
        INSERT INTO exchange_bank_branches (
            bank_name, branch_name, address, latitude, longitude, business_hours, phone_number, is_active
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.bankName},
                #{item.branchName},
                #{item.address},
                #{item.latitude},
                #{item.longitude},
                #{item.businessHours},
                #{item.phoneNumber},
                #{item.isActive}
            )
        </foreach>
    </insert>

</mapper>
