<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tripass.batch.mapper.MockTransactionMapper">

    <select id="selectActiveAccountIds" resultType="long">
        SELECT id FROM accounts WHERE is_deleted = 0
    </select>

    <insert id="insertMockTransaction" parameterType="TransactionMockDto">
        INSERT INTO transactions (
            account_id, category_id, trip_id, trip_country_id, currency_id,
            transaction_date, transaction_time, transaction_type, transaction_region,
            amount, balance_after, merchant_name, original_amount, applied_exchange_rate,
            payment_method, is_pre_expense, memo, external_key, is_deleted
        ) VALUES (
                     #{accountId}, #{categoryId}, #{tripId}, #{tripCountryId}, #{currencyId},
                     #{transactionDate}, #{transactionTime}, #{transactionType}, #{transactionRegion},
                     #{amount}, #{balanceAfter}, #{merchantName}, #{originalAmount}, #{appliedExchangeRate},
                     #{paymentMethod}, #{isPreExpense}, #{memo}, #{externalKey}, #{isDeleted}
                 )
    </insert>

</mapper>