<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tripass.dev.mapper.DevDateMapper">

    <select id="selectOverrideDate" resultType="java.time.LocalDate">
        SELECT dev_override_date
        FROM users
        WHERE id = #{userId}
          AND is_deleted = 0
    </select>

    <update id="updateOverrideDate">
        UPDATE users
        SET dev_override_date = #{overrideDate}
        WHERE id = #{userId}
          AND is_deleted = 0
    </update>

    <update id="clearOverrideDate">
        UPDATE users
        SET dev_override_date = NULL
        WHERE id = #{userId}
          AND is_deleted = 0
    </update>

    <select id="selectDemoRecordingMode" resultType="boolean">
        SELECT demo_recording_mode
        FROM users
        WHERE id = #{userId}
          AND is_deleted = 0
    </select>

    <update id="updateDemoRecordingMode">
        UPDATE users
        SET demo_recording_mode = #{enabled}
        WHERE id = #{userId}
          AND is_deleted = 0
    </update>

</mapper>
